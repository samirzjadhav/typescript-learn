var a = 12;
var b = 13;

let arr: [string, number] = ["samir", 12];

// enumeration example
enum UserRoles {
  ADMIN = "admin",
  GUEST = "guest",
  SUPER_ADMIN = "super_admin",
}

UserRoles.SUPER_ADMIN;
